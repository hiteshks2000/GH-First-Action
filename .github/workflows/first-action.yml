name: first-workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print-greeting
        run: |
            echo "Hello World"
            echo "Hello hitesh"
      - name: print-goodbye
        run: echo "Bye"
